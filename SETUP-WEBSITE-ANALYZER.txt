DoKitly Website Analyzer - Build 7
==================================

STATUS
The Website Analyzer frontend is already configured to use:
https://dokitly-analyzer.softcrafted.workers.dev

The connected Cloudflare Worker source is included at:
cloudflare-worker/website-analyzer-worker.js

If the Worker URL changes later, edit:
assets/website-analyzer-config.js

OPTIONAL PAGESPEED KEY
Google PageSpeed may return HTTP 429 when public quota is exhausted.
For more reliable PageSpeed results, add a Cloudflare Worker secret named:
PAGESPEED_API_KEY

Cloudflare Dashboard -> Workers & Pages -> dokitly-analyzer -> Settings -> Variables and Secrets.

NOTE
The main analyzer checks (HTTP, redirects, HTML SEO signals, robots/sitemap,
DNS, RDAP, security headers and technology signals) do not depend on the
PageSpeed quota.
